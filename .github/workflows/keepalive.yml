name: keepalive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping service
        run: curl -sf --max-time 30 https://bureau-theta.vercel.app/healthz || true
